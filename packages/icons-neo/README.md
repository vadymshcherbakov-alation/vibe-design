# icons-neo

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test icons-neo` to execute the unit tests via [Jest](https://jestjs.io).

## Best Practices for Adding New Icons

- Avoid hardcoding fill colors in the icons.
- Remove any instances of "fill none."
- Normalize viewBox sizes to align with other icons.
- Remove clip paths and IDs from the icons.
