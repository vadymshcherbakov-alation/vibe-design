# yaml

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build yaml` to build the library.

## Running unit tests

Run `nx test yaml` to execute the unit tests via [Vitest](https://vitest.dev/).
